apiVersion: v1
kind: Service
metadata:
  name: done-wise-adapter
  labels:
    app: done-wise-adapter
spec:
  selector:
    app: done-wise-adapter
  ports:
    - name: http
      port: 8080
      targetPort: http
