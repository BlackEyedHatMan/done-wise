apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: done-wise # ← change me
resources:
  - ../base
  - ingress.yml
# Pin the image for real deployments:
# images:
#   - name: ghcr.io/blackeyedhatman/done-wise-adapter
#     newTag: v1.0.0
